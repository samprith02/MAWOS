import { useState } from 'react';
import { Eye, EyeOff, GraduationCap, LockKeyhole, ShieldCheck, UserRound } from 'lucide-react';
import { useLocation, useNavigate } from 'react-router-dom';
import { useAuth } from '../../context/AuthContext';

const roles = [
  ['student', 'Student', 'Personal academic progress'], ['faculty', 'Faculty', 'Classes and assessments'],
  ['hod', 'HOD', 'Department insights'], ['principal', 'Principal', 'Institution overview'], ['admin', 'Admin', 'Operations and admissions'],
];
const landing = { student: '/student', faculty: '/faculty', hod: '/hod', principal: '/principal', admin: '/admin' };

export default function LoginPage() {
  const { login } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const [role, setRole] = useState('student'); const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [showPassword, setShowPassword] = useState(false); const [remember, setRemember] = useState(true); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  const submit = async (e) => { e.preventDefault(); setError(''); if (!username.trim() || !password) return setError('Enter your ID and password.'); setBusy(true); try { const user = await login(username.trim(), password); if (!remember) sessionStorage.setItem('mawos_session_only', 'true'); navigate(location.state?.from?.pathname || landing[user.role], { replace: true }); } catch (err) { setError(err.status === 401 ? 'Invalid credentials. Check your ID and password.' : err.message); } finally { setBusy(false); } };
  return <main className="grid min-h-screen bg-canvas lg:grid-cols-2"><section className="hidden flex-col justify-between bg-navy p-12 text-white lg:flex"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-primary text-lg font-bold">M</span><div><p className="text-lg font-bold">MAWOS</p><p className="text-sm text-slate-400">Multi-Agent Workflow Orchestration System</p></div></div><div><span className="mb-6 grid h-14 w-14 place-items-center rounded-2xl bg-blue-500/20 text-blue-200"><GraduationCap size={30} /></span><h1 className="max-w-lg text-4xl font-bold leading-tight">One trusted workspace for your academic journey.</h1><p className="mt-5 max-w-md leading-7 text-slate-300">Access your university data, workflows, and research services through a secure role-based portal.</p></div><div className="flex items-center gap-2 text-sm text-slate-400"><ShieldCheck size={16} />Secure JWT-authenticated access</div></section><section className="flex items-center justify-center p-5 sm:p-10"><form className="w-full max-w-xl rounded-2xl border bg-white p-6 shadow-card sm:p-8" onSubmit={submit}><div className="mb-7 lg:hidden"><div className="flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-lg bg-primary font-bold text-white">M</span><h1 className="text-xl font-bold">MAWOS</h1></div></div><h2 className="text-2xl font-bold">Welcome back</h2><p className="mt-1 text-sm text-muted">Choose your portal, then sign in with your institutional credentials.</p><fieldset className="mt-6"><legend className="mb-2 text-sm font-medium">Portal</legend><div className="grid grid-cols-1 gap-2 sm:grid-cols-2">{roles.map(([key, name, detail]) => <button type="button" key={key} onClick={() => setRole(key)} className={`rounded-lg border p-3 text-left transition ${role === key ? 'border-primary bg-blue-50 ring-1 ring-primary' : 'hover:border-slate-300'}`} aria-pressed={role === key}><span className="block text-sm font-semibold">{name}</span><span className="text-xs text-muted">{detail}</span></button>)}</div></fieldset><label className="label mt-5" htmlFor="username">User ID, USN, employee ID, or email</label><div className="relative"><UserRound className="absolute left-3 top-3 text-muted" size={18} /><input id="username" className="field pl-10" autoComplete="username" value={username} onChange={(e) => setUsername(e.target.value)} placeholder="Enter your ID" /></div><label className="label mt-4" htmlFor="password">Password</label><div className="relative"><LockKeyhole className="absolute left-3 top-3 text-muted" size={18} /><input id="password" className="field pl-10 pr-10" autoComplete="current-password" type={showPassword ? 'text' : 'password'} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Enter your password" /><button type="button" aria-label={showPassword ? 'Hide password' : 'Show password'} className="absolute right-3 top-3 text-muted" onClick={() => setShowPassword(!showPassword)}>{showPassword ? <EyeOff size={18} /> : <Eye size={18} />}</button></div><div className="mt-4 flex items-center justify-between text-sm"><label className="flex items-center gap-2 text-muted"><input type="checkbox" checked={remember} onChange={(e) => setRemember(e.target.checked)} />Remember me</label><button type="button" className="font-medium text-primary" onClick={() => setError('Password recovery is not available in the current backend API.')}>Forgot password?</button></div>{error && <p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button className="btn-primary mt-6 w-full" disabled={busy}>{busy ? 'Signing in…' : `Sign in to ${roles.find(([key]) => key === role)[1]} portal`}</button><p className="mt-4 text-center text-xs text-muted"><ShieldCheck className="mr-1 inline" size={14} />Your access is limited by your authenticated server role.</p></form></section></main>;
}

export { landing };
